server:
  port: 8090

spring:
  jpa:
    properties:
      hibernate:
        jdbc:
          batch_size: 50  # 한 번에 50개씩 배치로 저장
  main:
    allow-bean-definition-overriding: true
  data:
    mongodb:
      host: 호스트IP
      port: 25015
      username: ID
      authentication-database: admin
      password: PW
      database: DB
